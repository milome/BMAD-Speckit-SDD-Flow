# 弹窗测试阻塞改进 — Review Gaps 文档

**产出日期**: 2026-03-03  
**Review 对象**: `TASKS_弹窗测试阻塞改进_2026-03-03.md`  
**Party Mode 轮次**: 105 轮（批判性审计员发言 64 轮，> 60%）  
**收敛条件**: 最近 5 轮无新 gap，已收敛

---

## §1 问题描述类 Gaps

### G1.1 【高】「其他 chart/ui 测试」过于模糊

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | §1.2 表格最后一行「其他 chart/ui 测试 \| 各类 qapp/qtbot \| 大量依赖 Qt 的用例」未具体列举，执行者无法判断审查边界。实际 grep 显示 30+ 文件含 `def qapp` 或 `QApplication.instance`，与「其他」的模糊表述不一致。 |
| **修复建议** | 在 §1.2 中补充「其他」的完整清单，或明确引用「以 `grep -r "def qapp\|QApplication.instance" tests/` 输出为准」，与 T7 保持一致。 |
| **涉及章节** | §1.2 |

### G1.2 【中】图形界面/多周期图表覆盖表述分散

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | §1.2 表格中 ChartWindow、MultiTimeframeWidget 分散在多行，且「图形界面测试」以脚注形式说明「已纳入 T7 审查范围」，但未在问题描述中明确「多周期图表启动、模式切换」与弹窗阻塞的因果关系。 |
| **修复建议** | 在 §1.2 中增加一行或脚注：明确 ChartWindow/MultiTimeframeWidget 测试的阻塞场景（如 qtbot 与自定义 qapp 冲突、对话框 exec 阻塞等）。 |
| **涉及章节** | §1.2 |

### G1.3 【低】V2 与 CI 无头环境的表述矛盾

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **描述** | V2 要求「`pytest tests/` 完整全量（含 qt_dialog）在本地有显示器环境下可跑完」。但 §1.3 约束要求「必须兼容 CI 无头环境」。若完整全量仅限「有显示器」，则 CI 完整流水线在无头下跑 qt_dialog 可能仍阻塞，与「兼容无头」存在张力。 |
| **修复建议** | 在 §1.3 或 V2 中澄清：CI 无头环境下完整全量（含 qt_dialog）不保证通过；V2 仅验证「本地有显示器」场景。或明确 CI 完整流水线为可选、且建议有显示器环境。 |
| **涉及章节** | §1.3、§6 V2 |

---

## §2 根因分析类 Gaps

### G2.1 【高】子进程/多进程根因未在任务中体现

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | §2.1 根因表明确列出「子进程/多进程与 Qt 冲突」：OfflineTaskManagerUI 在 ComputeClient 子进程运行，测试中若误触发子进程或 IPC 与主进程 Qt 冲突。但 T3 仅改 mock QMessageBox，未涉及子进程隔离或「不触发子进程」的测试策略。 |
| **修复建议** | 在 T3 或新增任务中明确：test_offline_task_manager_ui 的测试需确保不实际启动 ComputeClient 子进程，或通过 mock IPC/子进程避免与主进程 Qt 冲突。或在 §2 中说明「子进程冲突」通过 mock 弹窗即可规避（若属实）。 |
| **涉及章节** | §2.1、T3 |

### G2.2 【中】QFileDialog、QColorDialog 等未覆盖

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | 根因与方案仅聚焦 QMessageBox、QDialog.exec()。代码库中存在 QFileDialog（如 price_line_event_monitor.py）、以及 QMessageBox 的多种用法（question、warning、critical、information）。mock_message_boxes 覆盖了 QMessageBox 四类，但未提及 QFileDialog、QColorDialog、QInputDialog 等可能阻塞的模态对话框。 |
| **修复建议** | 在 §2.1 或 §4.2.1 中补充：若测试会触发 QFileDialog/QColorDialog 等，采用相同策略（mock 或 qt_dialog 隔离）。或明确本改进范围仅限 QMessageBox/QDialog，其他模态框后续迭代。 |
| **涉及章节** | §2.1、§4.2.1 |

### G2.3 【低】无头环境在默认流水线下的残留风险

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **描述** | 根因表称「无头环境差异」易触发 access violation。默认流水线 `pytest tests/ -m "not qt_dialog"` 排除弹窗测试后，是否仍可能因其他 Qt 操作（如 ChartWindow、qtbot.addWidget）在无头下崩溃？未明确。 |
| **修复建议** | 在 §2 或 §4.2.4 中说明：排除 qt_dialog 后，剩余 Qt 测试在无头下预期可跑；若仍遇 access violation，需单独排查并可能打 qt_dialog 或进一步隔离。 |
| **涉及章节** | §2.1、§4.2.4 |

---

## §3 方案选型类 Gaps

### G3.1 【高】「既触发业务逻辑又验证布局」的判定规则缺失

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | §3.3 规则为：凡触发 QMessageBox/QDialog.exec() 的路径一律 mock；凡需验证「窗口显示、布局、用户点击对话框按钮」的一律隔离。但若某用例「既触发业务逻辑（如校验失败弹提示）又验证布局（如对话框按钮位置）」则如何判定？规则未覆盖。 |
| **修复建议** | 补充判定规则：优先 mock 业务逻辑，若同一用例需验证布局则打 qt_dialog 隔离；或拆分为两个用例（一个 mock 逻辑，一个隔离验证布局）。 |
| **涉及章节** | §3.3 |

### G3.2 【中】「窗口显示」是否包含 ChartWindow.show()

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | 判定规则中「凡需验证窗口显示、布局、用户点击对话框按钮的」——ChartWindow、MultiTimeframeWidget 的 show() 是否算「窗口显示」？若算，则 test_chart_window_mode_switch、test_compatibility_multi_timeframe 大量用例可能需打 qt_dialog，与「隔离用例数量应控制」存在张力。 |
| **修复建议** | 明确「窗口显示」指 QDialog/QMessageBox 等模态对话框的显示，不含 ChartWindow 等主窗口的 show；或明确 ChartWindow 相关测试的 qt_dialog 判定标准。 |
| **涉及章节** | §3.3、T7、T8 |

### G3.3 【中】完整全量命令与 addopts 的 override 方式未验证

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | T9 称 addopts 增加 `-m "not qt_dialog"` 后，默认 `pytest tests/` 即排除 qt_dialog；完整全量需「两阶段」或 `pytest tests/ -m ""` 覆盖。但 pytest 的 `-m ""` 是否有效覆盖 addopts 中的 `-m "not qt_dialog"` 未在文档中验证，不同版本可能行为不同。 |
| **修复建议** | 在 T9 或 T10 文档中明确：完整全量推荐使用 `pytest tests/ -m qt_dialog` 单独跑 qt_dialog 集，与 `pytest tests/ -m "not qt_dialog"` 两阶段组合；避免依赖 `-m ""` 覆盖 addopts。并验证 pytest 版本兼容性。 |
| **涉及章节** | §3.2、T9、T10 |

---

## §4 改进方案类 Gaps

### G4.1 【高】§4.2.3 与 T9 对「完整全量」表述矛盾

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | §4.2.3 写「完整全量（含弹窗）: `pytest tests/`」，即默认 `pytest tests/` 即含弹窗。但 T9 写 addopts 增加 `-m "not qt_dialog"` 后，「默认 `pytest tests/` 即排除 qt_dialog」——两者矛盾：若 addopts 含排除，则 `pytest tests/` 不再「完整全量」。 |
| **修复建议** | 统一表述：addopts 含 `-m "not qt_dialog"` 后，`pytest tests/` 为「默认全量（排除弹窗）」；「完整全量」为两阶段 `pytest tests/ -m "not qt_dialog"` + `pytest tests/ -m qt_dialog` 或等效。修正 §4.2.3 与 T9 的「完整全量」定义。 |
| **涉及章节** | §4.2.3、T9 |

### G4.2 【中】mock_message_boxes 是否覆盖 QMessageBox(...).exec()

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | T2 的 mock_message_boxes 使用 patch 替换 QMessageBox 的 warning、critical、information、question。但若业务代码使用 `QMessageBox(self).exec()` 或 `QMessageBox(...).exec()` 而非静态方法，mock 的 patch 目标可能不同，需 patch 类或实例方法。 |
| **修复建议** | 在 T2 中明确：mock 需覆盖 `QMessageBox.warning`、`critical`、`information`、`question` 的静态调用；若存在 `QMessageBox(...).exec()` 形式，需额外 patch 或纳入 mock 范围。建议在 T2 验收中增加「执行 grep 确认无未 mock 的 QMessageBox 调用路径」。 |
| **涉及章节** | §4.2.1、T2 |

### G4.3 【低】§4.2.2「其他」与 T7 清单的引用关系

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **描述** | §4.2.2 写「其他：逐个审查使用 qapp/qtbot 的测试」，未列出清单；T7 有冗长清单。§4 与 §5 的对应关系依赖读者自行关联。 |
| **修复建议** | 在 §4.2.2 中增加「详见 T7 涉及文件清单」的引用，避免重复维护。 |
| **涉及章节** | §4.2.2、T7 |

---

## §5 任务列表类 Gaps

### G5.1 【高】T7 与 T1、T2 的依赖关系未明确

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | T7 要求「审查并统一其他自定义 qapp 的测试」，但未说明 T1（标记注册）、T2（mock_message_boxes）是否需先完成。若 T7 审查中需对部分用例打 qt_dialog，则依赖 T1；若需使用 mock_message_boxes，则依赖 T2。 |
| **修复建议** | 在 T7 或任务依赖图中明确：T7 依赖 T1、T2 已完成；或 T7 可并行推进，但打 qt_dialog 时需 T1 已就绪，使用 mock 时需 T2 已就绪。 |
| **涉及章节** | T7、§5 任务列表 |

### G5.2 【高】T8 输入为空时的处理未定义

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | T8 输入为「T7 产出的需打 qt_dialog 的用例清单」。若 T7 审查后清单为空（所有用例均可 mock），T8 是否无任务？验收标准「`pytest tests/ -m qt_dialog` 可运行它们」——若清单为空，`pytest tests/ -m qt_dialog` 可能无用例，是否算通过？ |
| **修复建议** | 在 T8 中明确：若 T7 清单为空，T8 无操作，验收为「`pytest tests/ -m qt_dialog` 收集到 0 个用例且无报错」即通过。 |
| **涉及章节** | T8 |

### G5.3 【中】T3 验证方式中 grep 的跨平台兼容性

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | T3 验证方式写「执行 `grep -r "QMessageBox" tests/test_offline_task_manager_ui.py`」。Windows 下默认无 grep，需 Git Bash 或 WSL 或使用 `rg`/ripgrep。 |
| **修复建议** | 改为「执行 `grep -r` 或 `rg` 或 IDE 搜索 `QMessageBox`，列出所有调用点」；或明确「在 Git Bash / WSL 下执行」。 |
| **涉及章节** | T3 |

### G5.4 【中】T11「若适用」的验收标准模糊

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | T11 写「若项目存在含 pytest 或 tests 的 CI workflow」。实际存在 ci.yml（`pytest tests/ -v --no-cov`）。T11 验收「CI 默认流水线通过，无阻塞」——若 T11 被判定为「不适用」而跳过，则 CI 仍可能阻塞。 |
| **修复建议** | 明确 T11 适用条件：若存在 ci.yml 或等效且其中运行 pytest，则 T11 必须执行；验收为「CI 中 pytest 命令改为 `pytest tests/ -m "not qt_dialog"` 且流水线通过」。 |
| **涉及章节** | T11 |

### G5.5 【低】T7 涉及文件清单与 grep 实际输出的不一致风险

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **描述** | T7 列出 30+ 文件，并称「执行时以实际 grep 输出为准」。但 test_chart_phase3_4.py、test_chart_window_mode_switch.py 可能不包含 `def qapp` 或 `QApplication.instance`（它们使用 qtbot），grep 不会命中，却因 §1.2 需纳入审查。 |
| **修复建议** | 在 T7 中明确：审查范围 = grep 输出 ∪ §1.2 表格中所有文件（含无 qapp 但使用 qtbot/对话框的）。 |
| **涉及章节** | T7 |

---

## §6 验收标准类 Gaps

### G6.1 【高】V2 与 T10 的对应关系不明确

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | T10 要求「明确 V2（完整全量在本地有显示器下可跑完）与完整全量命令的对应关系」。但 V2 仅说「可跑完」，未说明具体命令。T10 文档需写明的「完整全量命令」应为两阶段 `pytest tests/ -m "not qt_dialog"` + `pytest tests/ -m qt_dialog`，且 V2 验收时需执行该两阶段。 |
| **修复建议** | 在 V2 中补充：验收时执行 `pytest tests/ -m "not qt_dialog"` 与 `pytest tests/ -m qt_dialog` 两阶段，均通过；在本地有显示器环境下。T10 文档需写清该两阶段与 V2 的对应关系。 |
| **涉及章节** | §6 V2、T10 |

### G6.2 【中】V3「业务断言不变」的验证方式可操作性不足

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | V3 要求「除因 mock 导致的预期行为变化外，业务断言不变；可通过执行前后对比关键用例的 assert 语句验证」。但「关键用例」未定义，「执行前后」指改进前后需保留 assert 快照，操作成本高。 |
| **修复建议** | 简化为：改进后 `pytest tests/ -m "not qt_dialog"` 全部通过，且无因 mock 引入的假阳性（如本应失败的用例因 mock 而通过）。或明确「关键用例」为 T3、T4、T5、T6 涉及的测试文件。 |
| **涉及章节** | §6 V3 |

### G6.3 【低】V2 与 V1 的包含关系

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **描述** | V1 为 `pytest tests/ -m "not qt_dialog"` 可跑完；V2 为完整全量（含 qt_dialog）在本地有显示器下可跑完。V2 包含 V1 的用例集合的超集，但验收时需分别执行两套命令。 |
| **修复建议** | 在 §6 中明确「V1 与 V2 需分别验收」即可，无需额外修改。 |
| **涉及章节** | §6 |

---

## §7 一致性/遗漏类 Gaps

### G7.1 【高】§4.2.3 与 T9「完整全量」表述不一致（与 G4.1 重复，合并）

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **描述** | 见 G4.1。 |
| **修复建议** | 见 G4.1。 |
| **涉及章节** | §4.2.3、T9 |

### G7.2 【中】CI 现有命令与 T11 的衔接

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | 当前 ci.yml 使用 `pytest tests/ -v --no-cov 2>&1 | head -100`，存在 `head -100` 截断输出，可能掩盖后续失败。T11 若仅改为 `pytest tests/ -m "not qt_dialog"`，未考虑移除 `head -100` 或保留完整输出。 |
| **修复建议** | 在 T11 中明确：CI 需移除 `head -100` 或确保测试失败时能正确上报；并将 pytest 命令改为 `pytest tests/ -m "not qt_dialog" -v --no-cov`。 |
| **涉及章节** | T11、ci.yml |

### G7.3 【中】pytest-qt 与 pytest.ini 的 --strict-markers 兼容性

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **描述** | pytest.ini 已有 `--strict-markers`，未注册的 marker 会报错。T1 注册 `qt_dialog` 后，T8 打的 `@pytest.mark.qt_dialog` 才能通过。若 T1 遗漏或 T8 先于 T1 执行，会失败。 |
| **修复建议** | 在 T1 验收中明确：`pytest --markers` 输出包含 `qt_dialog`，且 `pytest tests/ -m qt_dialog` 可运行（即使 0 个用例）不报 strict-markers 错误。 |
| **涉及章节** | T1、pytest.ini |

### G7.4 【低】test_chart_phase3_4、test_chart_window_mode_switch 的 lastfailed 状态

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **描述** | .pytest_cache 显示 test_chart_window_mode_switch 多个用例在 lastfailed 中，可能为阻塞或失败。本改进是否预期解决这些用例？若 mock/隔离后仍失败，是否需单独排查？ |
| **修复建议** | 在 T7 或文档中说明：若审查后某文件仍因非弹窗原因失败，需单独排查并可能排除在本次改进范围外；或记录为已知问题。 |
| **涉及章节** | T7 |

---

## 附录：105 轮讨论摘要（按阶段压缩）

### 轮次 1–20：§1 问题描述质疑

- **批判性审计员（×12）**：影响范围「其他 chart/ui 测试」过于模糊；图形界面与多周期图表的因果关系未明确；V2 与 CI 无头环境的约束存在张力。
- **Winston**：§1.2 表格可引用 T7 的 grep 输出，保持一致性。
- **Amelia**：test_chart_window_mode_switch 使用 qtbot，可能无自定义 qapp，但仍需审查。
- **Quinn**：需区分「阻塞」与「失败」——lastfailed 中的用例可能是失败而非阻塞。
- **Murat**：无头 + 弹窗是已知风险，隔离后默认不跑可缓解。

### 轮次 21–40：§2 根因分析质疑

- **批判性审计员（×14）**：子进程根因未在任务中体现；QFileDialog 等未覆盖；无头环境下默认流水线是否仍有残留风险。
- **Winston**：子进程冲突可通过 mock 弹窗或避免触发子进程解决，需在 T3 或 §2 中明确。
- **Amelia**：mock_message_boxes 的 patch 目标需覆盖 QMessageBox 的各类用法。
- **Quinn**：QFileDialog 若在测试中触发，同样会阻塞，建议扩展范围或明确排除。
- **Murat**：根因表可补充「其他模态框」后续迭代的说明。

### 轮次 41–60：§3 方案选型质疑

- **批判性审计员（×12）**：mock 与隔离的边界不清晰；「既触发逻辑又验证布局」无规则；ChartWindow.show 是否算「窗口显示」；完整全量命令与 addopts 的 override 未验证。
- **Winston**：建议优先 mock，布局验证单独隔离；ChartWindow 主窗口与 QDialog 模态框应区分。
- **Amelia**：pytest 的 `-m ""` 可能无法覆盖 addopts，建议两阶段跑法。
- **Quinn**：qt_dialog 用例数量应控制，避免「隔离成主集」。
- **Murat**：两阶段跑法更稳妥，不依赖 addopts override。

### 轮次 61–80：§4–§5 改进方案与任务列表质疑

- **批判性审计员（×14）**：§4.2.3 与 T9 对「完整全量」矛盾；T7 与 T1、T2 依赖未明确；T8 清单为空时未定义；T3 的 grep 跨平台；T11 适用条件模糊。
- **Winston**：统一「完整全量」为两阶段；T7 依赖 T1、T2。
- **Amelia**：T8 空清单时验收为「0 个用例无报错」；T3 改用「grep 或 rg 或 IDE 搜索」。
- **Quinn**：T11 若存在 ci.yml 则必须执行。
- **Murat**：T7 审查范围 = grep ∪ §1.2 表格。

### 轮次 81–100：§6 验收标准与一致性质疑

- **批判性审计员（×12）**：V2 与 T10 对应关系不明确；V3 可操作性不足；CI 的 head -100 截断问题；strict-markers 与 qt_dialog 的兼容性。
- **Winston**：V2 验收需执行两阶段命令；T10 文档写清对应关系。
- **Amelia**：V3 可简化为「全部通过且无假阳性」。
- **Quinn**：T11 需移除 head -100 或确保失败可上报。
- **Murat**：T1 验收需包含 strict-markers 下 qt_dialog 可用。

### 轮次 101–105：收敛

- **批判性审计员**：Gaps 已覆盖 §1–§7，无新 gap。
- **Winston**：同意收敛。
- **Amelia**：同意收敛。
- **Quinn**：同意收敛。
- **Murat**：同意收敛。

**收敛确认**：最近 5 轮无人提出新 gap，讨论结束。

---

## Gaps 汇总统计

| 严重程度 | 数量 |
|----------|------|
| 高 | 8 |
| 中 | 11 |
| 低 | 5 |
| **合计** | **24** |

---

**文档结束**
