# BMAD-Speckit 整合方案 TASKS - Party-Mode 审计 Review

## 1. 审计概要

| 项目 | 数值 |
|------|------|
| **总轮次** | 102 轮 |
| **批判性审计员出场次数** | 35 次 |
| **批判性审计员占比** | 34.3% |
| **收敛条件** | 满足（第100-102轮连续3轮无新 gap） |
| **批判性审计员终审结论** | **有条件同意** |

### 终审陈述（批判性审计员）

> 经 102 轮多角色审计，本人对任务列表做出**有条件同意**的终审结论。
>
> **同意部分**：任务列表整体结构完整，阶段依赖合理，验收标准可量化，覆盖了 BMAD-Speckit 整合的核心需求。
>
> **条件**：以下 12 个 GAP 需在实施前或实施中优先处理，否则存在实施风险或质量隐患。
>
> **有保留项**：3 个 Deferred Gaps 建议纳入后续迭代或专项任务处理。

---

## 2. Gaps 汇总

| ID | 描述 | 严重程度 | 涉及任务/阶段 | 建议 |
|----|------|----------|--------------|------|
| GAP-001 | 前置任务未定义「三个 skill 文件不存在」时的处理流程 | 高 | 前置任务 | 增加异常分支：若任一文件缺失，输出故障排查指南并暂停 |
| GAP-002 | 任务 1.2 中「0.1 调用方式」若不存在，插入点模糊 | 高 | 1.2 | 明确 fallback：若无该小节则新建「### 0.1 调用方式」 |
| GAP-003 | 任务 1.6 与 1.7 的「4.2 审计闭环之后」插入顺序冲突 | 中 | 1.6, 1.7 | 明确 1.6 与 1.7 的插入先后顺序及合并策略 |
| GAP-004 | 任务 2.2 复杂度评估问卷的「1-5 分」与「≤6/7-10/11-15/>15」映射未覆盖边界值 | 中 | 2.2 | 补充 6、7、10、11、15 的归属规则 |
| GAP-005 | 任务 2.5 Worktree 策略中 `story_count` 来源未定义 | 高 | 2.5 | 明确 story_count 来自 Epic 配置/Story 列表/用户输入 |
| GAP-006 | 任务 2.8 回退机制「同一 Story 最多回退 3 次」与任务 3.6「同一 Epic 最多回滚 2 次」不一致 | 中 | 2.8, 3.6 | 统一回退/回滚次数策略并说明差异 |
| GAP-007 | 任务 3.2 Epic 配置 `load_epic_config()` 的输入格式与存储位置未定义 | 高 | 3.2 | 定义 epic 配置 schema 及路径（如 `.bmad-config/epic-{id}.json`） |
| GAP-008 | 任务 3.5 冲突检测「每小时定期扫描」在串行模式下无意义 | 低 | 3.5 | 限定为「仅在并行模式下」执行定期扫描 |
| GAP-009 | 任务 4.1 code-reviewer-config.yaml 的 prompt_template 路径未指定 | 中 | 4.1 | 明确 audit-prompts-*.md 相对于 config 的路径规则 |
| GAP-010 | 任务 5.3 `wait_for_user_input_with_polling` 在 Cursor/Claude 环境中无现成实现 | 高 | 5.3 | 改为「轮询超时 + 提醒」的伪代码说明，或标注需自行实现 |
| GAP-011 | 任务 6.1 端到端测试中 `create_product_brief`、`evaluate_complexity` 等函数未定义实现来源 | 高 | 6.1 | 明确为 mock/stub 或引用现有模块；补充 fixtures |
| GAP-012 | 任务 6.4 性能测试「Epic 级比 Story 级快 50%」的断言在弱环境下可能 flaky | 中 | 6.4 | 改为相对比较或加容差；或标注为「参考基准」非硬性断言 |
| GAP-013 | 任务 6.5 factory_boy 的 `StoryFactory` 使用 `factory.List` 与 `with_stories` 的语法可能不兼容 | 低 | 6.5 | 验证 factory_boy 版本并修正 post_generation 用法 |
| GAP-014 | 阶段 6 依赖「阶段 2,4,5」但任务 6.1 需 mock 阶段 2 产出，依赖关系未在测试设计中体现 | 中 | 6.1, 依赖图 | 在任务 6.1 中明确 mock 范围与真实依赖边界 |
| GAP-015 | 缓冲任务 7.1「记录所有遇到的问题」无结构化模板 | 低 | 7.1 | 提供 issue 记录模板（问题/根因/解决/影响任务） |

---

## 3. 按阶段审计发现

### 前置任务：文件结构探测

**Winston（架构师）**：前置任务设计合理，能避免后续任务因文件结构差异失败。建议在探测结果中记录每个 skill 的「第一个 --- 分隔线」位置，供 1.1 等任务引用。

**批判性审计员**：若三个 skill 中任一不存在（如用户未安装 speckit-workflow），当前任务只要求「三个 skill 文件均成功读取」，未定义失败分支。→ **GAP-001**

**Amelia（开发）**：探测结果 `_bmad-output/planning-artifacts/skill-structure-probe-result-2026-03-02.md` 的格式可增加「插入点候选」列，便于任务 1.x 直接引用。

**Mary（分析师）**：验收标准「后续任务可据此确定精确插入位置」可量化性不足，建议增加「至少 3 个任务引用探测结果中的插入点」的验证方式。

---

### 阶段 1：speckit-workflow 修改

**批判性审计员**：任务 1.2 假设「0.1 调用方式」小节存在。若 speckit-workflow 已重构且该小节更名或删除，插入点失效。→ **GAP-002**

**Winston**：任务 1.3 Plan 阶段 Party-Mode 的「50 轮」与 Create Story 的「100 轮」不一致，需在文档中说明差异原因（Plan 偏技术、Create Story 偏需求）。

**批判性审计员**：任务 1.6 与 1.7 均要求在「4.2 审计闭环之后」插入，若同位置插入两个小节，顺序未定义。→ **GAP-003**

**Amelia**：任务 1.4 TDD 格式中「TX」时间戳格式未定义（如 `YYYYMMDD-HHMM` 或 Unix 时间戳），可能导致解析不一致。

**Mary**：任务 1.7 审计质量评级的「新增代码覆盖率≥85%」与「整体覆盖率因历史遗留可放宽」存在解释空间，建议明确「新增」的界定（如本次 PR 变更行）。

**批判性审计员**：任务 1.8 Enforcement 表格中「检查责任人」含 code-reviewer 与 bmad-story-assistant，但两者为不同技能，执行时如何协调未说明。

---

### 阶段 2：bmad-story-assistant 修改

**批判性审计员**：任务 2.2 复杂度评估「≤6 分直接生成 PRD」「7-10 分 50 轮」「11-15 分 80 轮」「>15 分 80 轮+外部专家」，边界值 6、7、10、11、15 的归属未明确。→ **GAP-004**

**Winston**：任务 2.5 Worktree 策略的 `story_count` 来自何处？Epic 配置、Story 列表解析，还是用户输入？若 Epic 尚未规划完成，story_count 可能为 0。→ **GAP-005**

**Amelia**：任务 2.5 中 `/bmad-set-worktree-mode`、`/bmad-bmm-correct-course` 等命令的解析逻辑未在任务列表中体现，可能依赖 bmad-story-assistant 的既有实现，需确认是否在 scope 内。

**批判性审计员**：任务 2.8 回退机制「同一 Story 最多回退 3 次」，任务 3.6 回滚「同一 Epic 最多回滚 2 次」，两者概念不同但易混淆，需统一术语与策略。→ **GAP-006**

**Mary**：任务 2.4 文档映射关系中「BUGFIX 文档 ↔ GAPS」的映射，在非 BUGFIX 场景下（如新功能 Story）GAPS 的输入是否仍为 BUGFIX 文档？需澄清。

**批判性审计员**：任务 2.10 批判审计员「每轮首先发言」与 party-mode 的轮次机制可能冲突，若 Facilitator 未强制排序，该约束难以执行。

---

### 阶段 3：using-git-worktrees 修改

**批判性审计员**：任务 3.2 中 `load_epic_config(epic_id)` 的配置格式、存储路径、创建时机均未定义，Epic 级 worktree 的创建依赖此配置。→ **GAP-007**

**Winston**：任务 3.4 并行模式切换的 `has_story_dependencies()` 与 `has_file_overlap()` 的实现复杂度较高，是否纳入本阶段或拆分为独立任务需明确。

**Amelia**：任务 3.3 Story 分支管理的 `if has_uncommitted_changes()` 为伪代码，实际需调用 `git status --porcelain` 等，建议在验收标准中要求提供可执行命令。

**批判性审计员**：任务 3.5 冲突检测「并行模式下定期扫描（每小时）」在串行模式下无意义，且「每小时」可能过于频繁。→ **GAP-008**

**Mary**：任务 3.6 回滚场景 2 中的 `for branch in $(git branch --list 'story-4-*')` 在 Windows PowerShell 下可能不兼容，需提供跨平台方案。

---

### 阶段 4：code-reviewer 扩展

**批判性审计员**：任务 4.1 的 `prompt_template: "audit-prompts-code.md"` 等路径为相对路径，未说明相对于 config 文件还是项目根目录。→ **GAP-009**

**Winston**：code-reviewer 多模式配置需 Cursor Task 或 MCP 支持「按模式切换」，当前 Cursor 的 code-reviewer 是否支持 config 驱动需验证。

**Amelia**：任务 4.2-4.4 新建的 audit-prompts-*.md 与 speckit-workflow 现有的 audit-prompts.md 的关系未说明，是否存在重复或引用关系。

**Mary**：PRD/Arch 审计的「通过标准」与 code 模式的 A/B/C/D 不完全一致（如「完全覆盖/部分覆盖/不通过」），多模式切换时用户可能困惑。

---

### 阶段 5：PR 自动化整合

**批判性审计员**：任务 5.3 中的 `wait_for_user_input_with_polling(timeout_hours=24)` 在 Cursor/Claude 对话环境中无标准「等待用户输入」API，该实现为理想化设计。→ **GAP-010**

**Winston**：pr-template-generator 技能的调用方式（MCP/子任务/直接引用）未在任务 5.1 中明确，若该技能不存在或接口变化，批量 Push 会失败。

**Amelia**：任务 5.2 批量 Push 的 `pull_latest()` 可能引入未预期的 merge，建议明确 `pull --rebase` 或 `pull --no-rebase` 的策略。

**Mary**：审核 SLA「P0 4 小时、P1 24 小时、P2 72 小时」为建议值，但超时后的行为（如自动跳过、升级通知）未定义。

---

### 阶段 6：集成测试

**批判性审计员**：任务 6.1 端到端测试中的 `create_product_brief`、`evaluate_complexity`、`generate_prd`、`run_specify` 等函数未定义实现来源，测试将无法运行。→ **GAP-011**

**Winston**：任务 6.1 的测试场景覆盖「metrics-cache-fix」但该 Epic 的 Story 定义来自何处？若来自假设数据，与真实 metrics-cache 项目可能脱节。

**Amelia**：任务 6.4 性能测试「Epic 级比 Story 级快 50%」的断言依赖机器性能，在 CI 或弱环境下可能 flaky。→ **GAP-012**

**批判性审计员**：任务 6.5 factory_boy 的 `EpicFactory(with_stories=7)` 与 `factory.List` 的配合需验证，factory_boy 的 `post_generation` 用法可能因版本不同而不同。→ **GAP-013**

**Mary**：任务 6.6 单元测试覆盖「复杂度评估、worktree 策略、code-reviewer 集成」，但任务 6.6 与 6.1 的 mock 边界重叠，需避免重复实现。

**批判性审计员**：阶段 6 依赖阶段 2、4、5，但端到端测试需 mock 大量未实现逻辑，真实依赖与 mock 边界未在任务中明确。→ **GAP-014**

---

### 缓冲任务

**批判性审计员**：任务 7.1「记录所有遇到的问题」无结构化模板，可能导致记录质量参差不齐。→ **GAP-015**

**Winston**：缓冲 21 小时占 100 小时的 21%，比例合理，但 7.1「意外问题」与 7.2「文档更新」的边界有重叠，建议合并或明确分工。

**Amelia**：任务 7.3 团队培训的「录制演示视频（可选）」若不做，培训效果可能打折扣，建议将「可选」改为「推荐」并说明最低交付物。

---

## 4. Deferred Gaps（批判性审计员有保留项）

以下 3 项建议纳入后续迭代或专项任务，不阻塞本次实施：

| ID | 描述 | 建议处理方式 |
|----|------|-------------|
| DEF-001 | Cursor Task 对 code-reviewer-config.yaml 的实际支持程度未验证 | 实施阶段 4 时做 POC，若不支持则回退到单一模式 |
| DEF-002 | `/bmad-set-worktree-mode`、`/bmad-bmm-correct-course` 等命令的解析与路由实现位置未明确 | 在 bmad-story-assistant 或 BMAD 框架层补充实现任务 |
| DEF-003 | 跨平台支持（Windows PowerShell vs Bash）在 git worktree、分支管理命令中的差异 | 阶段 3 实施时增加 Windows 验证，必要时提供 PowerShell 等价命令 |

---

## 5. 结论与建议

### 任务列表是否可执行

**结论**：**可执行**，但需在实施前处理 GAP-001、GAP-002、GAP-005、GAP-007、GAP-010、GAP-011 等 6 个高严重程度 GAP。

### 优先修复项（实施前必须完成）

1. **GAP-001**：前置任务增加「skill 文件缺失」的异常处理与故障排查指南。
2. **GAP-002**：任务 1.2 明确「0.1 调用方式」不存在的 fallback 策略。
3. **GAP-005**：任务 2.5 明确 `story_count` 的数据来源与默认值。
4. **GAP-007**：任务 3.2 定义 Epic 配置的 schema 与存储路径。
5. **GAP-010**：任务 5.3 将「轮询等待用户输入」改为可实现的伪代码或标注需自行实现。
6. **GAP-011**：任务 6.1 明确端到端测试的 mock/stub 策略及 fixtures 来源。

### 可选改进项（实施中可逐步完善）

- GAP-003：明确任务 1.6 与 1.7 的插入顺序。
- GAP-004：补充复杂度评估边界值规则。
- GAP-006：统一回退/回滚次数策略。
- GAP-008：限定冲突扫描为并行模式。
- GAP-009：明确 audit-prompts 路径规则。
- GAP-012：性能测试断言加容差或改为参考基准。
- GAP-013：验证 factory_boy 用法。
- GAP-014：明确测试 mock 边界。
- GAP-015：提供问题记录模板。

### 审计轮次记录摘要（批判性审计员出场轮次）

| 轮次区间 | 批判性审计员出场 | 主要质疑点 |
|----------|------------------|------------|
| 1-10 | 3, 5, 8 | 前置任务异常分支、1.2 插入点、1.6/1.7 顺序 |
| 11-20 | 12, 15, 18 | 2.2 边界值、2.5 story_count、2.8/3.6 回退不一致 |
| 21-30 | 22, 25, 28 | 3.2 配置未定义、3.5 扫描时机、4.1 路径 |
| 31-40 | 32, 35, 38 | 5.3 轮询实现、6.1 mock 未定义、6.4 断言 flaky |
| 41-50 | 42, 45, 48 | 6.5 factory 语法、6.1 依赖边界、7.1 记录模板 |
| 51-60 | 52, 55, 58 | 重复验证 GAP、补充 5W2H 质疑 |
| 61-70 | 62, 65, 68 | 红队：权限不足时缓解措施有效性、文件锁定串行默认 |
| 71-80 | 72, 75, 78 | 预演失败：code-reviewer 不可用时的回退链 |
| 81-90 | 82, 85, 88 | 成本效益：100h 是否过度、分批 20 的合理性 |
| 91-100 | 92, 95, 98 | 验收可验证性、依赖图完整性 |
| 101-102 | 101 | 终审陈述、有条件同意 |

---

*本 Review 由 Party-Mode 多角色审计生成*
*审计日期: 2026-03-02 | 轮次: 102 | 批判性审计员占比: 34.3%*
