# TASKS.md 审计报告（audit-prompts.md §5）

## 1. 审计概要

| 项目 | 数值 |
|------|------|
| **审计依据** | specs\bmad-speckit-integration-TASKS-party-mode-review.md（GAP-001～GAP-015）、specs\bmad-speckit-integration-FINAL-COMPLETE.md |
| **审计对象** | specs\bmad-speckit-integration-TASKS.md（v1.1，40 任务，100h） |
| **模拟轮次** | 5 轮（多角色 + 批判性审计员） |
| **批判性审计员出场次数** | 2 次 |
| **批判性审计员占比** | 40%（2/5 轮） |
| **收敛情况** | 第 4～5 轮连续 2 轮无新 gap，满足「最近 2-3 轮无人提出新 gap」 |
| **终审结论** | **完全覆盖、验证通过**（见下文终审陈述） |

### 批判性审计员终审陈述

> 经逐条对照 GAP-001～GAP-015 及 FINAL-COMPLETE.md，本人对 TASKS.md 做出**完全覆盖、验证通过**的终审结论。
>
> **理由**：任务列表对 party-mode-review 中列出的 15 个 GAP 均存在显式修复标注或对应修改内容；与 FINAL-COMPLETE 的五层架构、复杂度矩阵、Worktree 策略、回退/回滚区分、强制人工审核等设计一致；任务依赖、验收标准与阶段划分可作为实施依据。
>
> **保留意见**：DEF-001～DEF-003（Deferred Gaps）未在 TASKS 中单独成任务，与 party-mode-review 的「不阻塞本次实施」一致，建议在阶段 4/3 实施时做 POC 或 Windows 验证并记录于 7.1 模板。

---

## 2. 逐条检查与验证结果

### GAP-001：前置任务「三个 skill 文件不存在」时的处理流程

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 增加异常分支：若任一文件缺失，输出故障排查指南并暂停 |
| **TASKS 对应位置** | 前置任务「文件结构探测」→ **异常分支（GAP-001 修复）**（约第 47～50 行） |
| **验证内容** | 是否定义「任一 skill 不存在」时的输出与暂停行为 |
| **验证结果** | **通过**。TASKS 明确：若任一 skill 文件不存在则 (1) 输出故障排查指南（检查 skills 目录、提示安装或从 _bmad 引用）；(2) 暂停后续阶段，不得执行阶段 1 及以后；(3) 在探测结果中记录缺失文件清单。验收标准含「三个 skill 文件均成功读取，或异常分支已执行」。 |

---

### GAP-002：任务 1.2「0.1 调用方式」不存在的 fallback

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 明确 fallback：若无该小节则新建「### 0.1 调用方式」 |
| **TASKS 对应位置** | 任务 1.2 **具体位置（GAP-002 修复）**（约第 118 行） |
| **验证内容** | 是否在「具体位置」中写明 fallback 策略 |
| **验证结果** | **通过**。TASKS 写明：在「§0 技能依赖」内定位「### 0.1 调用方式」，若该小节不存在则**新建「### 0.1 调用方式」小节**并插入优先/回退策略内容。 |

---

### GAP-003：任务 1.6 与 1.7 在「4.2 审计闭环之后」的插入顺序

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 明确 1.6 与 1.7 的插入先后顺序及合并策略 |
| **TASKS 对应位置** | 任务 1.6（约第 284 行）、任务 1.7（约第 329 行） |
| **验证内容** | 是否规定先插 1.6 再插 1.7，且顺序在文中一致 |
| **验证结果** | **通过**。1.6 规定「在 4.2 审计闭环之后**首先**插入本小节（任务分批执行机制），任务 1.7 紧随其后」；1.7 规定「紧随任务 1.6『任务分批执行机制』之后插入」，并写明顺序：4.2 审计闭环 → 1.6 任务分批 → 1.7 审计质量评级。 |

---

### GAP-004：任务 2.2 复杂度评估边界值（6、7、10、11、15）归属

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 补充 6、7、10、11、15 的归属规则 |
| **TASKS 对应位置** | 任务 2.2 Step 3「根据总分决定PRD生成方式（GAP-004 修复：边界值归属规则）」（约第 490～494 行） |
| **验证内容** | 是否明确 ≤6、7-10、11-15、>15 的边界归属 |
| **验证结果** | **通过**。TASKS 写明：≤6 分（含 6）；7-10 分（含 7、10）；11-15 分（含 11、15）；>15 分（不含 15）。与 FINAL-COMPLETE 的 Party-Mode 触发规则一致。 |

---

### GAP-005：任务 2.5 Worktree 策略中 story_count 来源

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 明确 story_count 来自 Epic 配置/Story 列表/用户输入 |
| **TASKS 对应位置** | 任务 2.5「**story_count 来源（GAP-005 修复）**」（约第 734 行）、任务 3.2 步骤 1（约第 1175 行） |
| **验证内容** | 是否定义优先级与默认值 |
| **验证结果** | **通过**。TASKS 明确：(1) Epic 配置 `epic.story_count`；(2) Story 列表 `len(epic.stories)`；(3) 用户输入 `--story-count N`；若 Epic 尚未规划完成默认 `story_count=0`，采用 story-level。3.2 中再次一致：`story_count = epic.story_count or len(epic.stories) or user_input`，默认 0。 |

---

### GAP-006：任务 2.8 回退与任务 3.6 回滚次数策略一致性与区分

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 统一回退/回滚次数策略并说明差异 |
| **TASKS 对应位置** | 任务 2.8「**回退限制（GAP-006 修复：与回滚区分）**」（约第 1015～1021 行）、任务 3.6「**回滚限制（GAP-006 修复：与回退区分）**」（约第 1707～1712 行） |
| **验证内容** | 是否区分概念并统一术语（回退按 Story 3 次、回滚按 Epic 2 次） |
| **验证结果** | **通过**。TASKS 明确：**回退**（correct-course）按 Story 计，同一 Story 最多 3 次；**回滚**（rollback-worktree）按 Epic 计，同一 Epic 最多 2 次；并互相引用 2.8/3.6 说明差异，与 FINAL-COMPLETE 一致。 |

---

### GAP-007：任务 3.2 Epic 配置 load_epic_config() 的格式与路径

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 定义 epic 配置 schema 及路径（如 .bmad-config/epic-{id}.json） |
| **TASKS 对应位置** | 任务 3.2「**步骤1：检测Epic特征（GAP-007 修复：Epic 配置 schema）**」（约第 1224～1230 行） |
| **验证内容** | 是否给出路径与 schema |
| **验证结果** | **通过**。TASKS 明确：路径 `.bmad-config/epic-{epic_id}.json`；Schema 注释：`{ "epic_id": int, "story_count": int, "stories": [...], "mode": "serial"|"parallel" }`；`load_epic_config(epic_id)` 从该路径读取。 |

---

### GAP-008：任务 3.5 冲突检测「每小时定期扫描」限定为并行模式

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 限定为「仅在并行模式下」执行定期扫描 |
| **TASKS 对应位置** | 任务 3.5「**自动检测时机（GAP-008 修复）**」（约第 1476～1479 行） |
| **验证内容** | 是否写明串行模式下不执行定期扫描 |
| **验证结果** | **通过**。TASKS 写明：自动检测时机包含「**仅在并行模式下**：定期扫描（每小时）；串行模式下无意义，不执行」。 |

---

### GAP-009：任务 4.1 code-reviewer-config 的 prompt_template 路径规则

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 明确 audit-prompts-*.md 相对于 config 的路径规则 |
| **TASKS 对应位置** | 任务 4.1「# GAP-009 修复：prompt_template 路径规则」（约第 1780～1783 行） |
| **验证内容** | 是否说明相对路径基准或绝对路径示例 |
| **验证结果** | **通过**。TASKS 在配置注释中明确：路径相对于 code-reviewer-config.yaml 所在目录；或使用绝对路径示例（speckit-workflow\references\audit-prompts-*.md）。 |

---

### GAP-010：任务 5.3 wait_for_user_input_with_polling 在 Cursor/Claude 中无现成实现

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 改为「轮询超时 + 提醒」的伪代码说明，或标注需自行实现 |
| **TASKS 对应位置** | 任务 5.3 单 PR 审核界面代码块（约第 2645～2655 行）、批量审核（约第 2704 行） |
| **验证内容** | 是否标注伪代码/需自行实现及实现建议 |
| **验证结果** | **通过**。TASKS 注释明确：GAP-010 修复，Cursor/Claude 无现成 API，需自行实现；实现建议为输出 prompt 后结束本轮，用户下条消息回复，超时后发送提醒；并标注「以下为伪代码，实际需根据运行环境实现」。批量处再次引用「见 GAP-010 实现说明」。 |

---

### GAP-011：任务 6.1 端到端测试中 create_product_brief 等函数实现来源

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 明确为 mock/stub 或引用现有模块；补充 fixtures |
| **TASKS 对应位置** | 任务 6.1 文件头「**GAP-011 修复**」（约第 2758～2763 行） |
| **验证内容** | 是否列出 mock/stub 清单及定义位置 |
| **验证结果** | **通过**。TASKS 明确：以下函数为 mock/stub，需在 conftest.py 或本文件 fixtures 中定义：create_product_brief, evaluate_complexity, generate_prd, generate_architecture, create_epic, split_stories, analyze_dependencies, determine_worktree_strategy, create_story, load_story, run_specify, run_plan, run_gaps, run_tasks, run_tests, code_review。 |

---

### GAP-012：任务 6.4 性能测试「Epic 级比 Story 级快 50%」断言的 flaky 与容差

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 改为相对比较或加容差；或标注为「参考基准」非硬性断言 |
| **TASKS 对应位置** | 任务 6.4 性能测试代码（约第 3335～3337、3367、3402 行） |
| **验证内容** | 是否使用容差或参考基准表述 |
| **验证结果** | **通过**。TASKS 注释为 GAP-012 修复：改为相对比较，弱环境可能 flaky；验证 Epic 级更快（至少快 30%，容差 0.7；或标注为参考基准非硬性断言）；验收标准为「参考基准，容差 0.7；见 GAP-012」。 |

---

### GAP-013：任务 6.5 factory_boy StoryFactory 与 factory.List / with_stories 兼容性

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 验证 factory_boy 版本并修正 post_generation 用法 |
| **TASKS 对应位置** | 任务 6.5 文件头与 EpicFactory（约第 3417～3433 行） |
| **验证内容** | 是否说明版本要求或替代写法 |
| **验证结果** | **通过**。TASKS 注释为 GAP-013 修复：验证 factory_boy 版本（支持 post_generation）；factory.List 可能需改为 factory.LazyFunction 或 SubFactory 链；with_stories 需与 post_generation 签名兼容；代码已使用 `stories = factory.LazyFunction(list)` 替代 factory.List([])。 |

---

### GAP-014：阶段 6 依赖与任务 6.1 mock 范围、真实依赖边界

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 在任务 6.1 中明确 mock 范围与真实依赖边界 |
| **TASKS 对应位置** | 任务 6.1「**GAP-014 修复**」（约第 2762 行） |
| **验证内容** | 是否区分 mock 范围与真实依赖 |
| **验证结果** | **通过**。TASKS 明确：mock 范围 = Layer 1-3 产出（Product Brief/PRD/Arch/Epic/Story）；真实依赖 = 阶段 4、5 的 code-reviewer、speckit 流程。与阶段 6 依赖「阶段 2,4,5」一致，2 为 Layer 1-3 流程产出，4/5 为真实依赖。 |

---

### GAP-015：缓冲任务 7.1「记录所有遇到的问题」的结构化模板

| 检查项 | 内容 |
|--------|------|
| **GAP 要求** | 提供 issue 记录模板（问题/根因/解决/影响任务） |
| **TASKS 对应位置** | 任务 7.1「**GAP-015 修复：问题记录模板**」（约第 3652～3658 行） |
| **验证内容** | 是否给出表格模板 |
| **验证结果** | **通过**。TASKS 提供表格模板：\| 问题 \| 根因 \| 解决方案 \| 影响任务 \|，并规定每遇到问题须按此格式记录；验收标准含「使用上述模板」。 |

---

### 与 FINAL-COMPLETE.md 的一致性抽查

| 设计要点 | FINAL-COMPLETE | TASKS.md 对应 | 结果 |
|----------|----------------|---------------|------|
| 五层架构 | Layer 1～5 定义 | 阶段零/Layer 2/阶段三/四、阶段 5/6 对应 | 一致 |
| 复杂度矩阵 | ≤6/7-10/11-15/>15，Party-Mode 轮次 | 任务 2.2 边界值 + 50/80 轮 | 一致 |
| Worktree 策略 | Story≤2 用 Story 级，≥3 用 Epic 级 | 任务 2.5、3.1、3.2 | 一致 |
| 回退 vs 回滚 | correct-course / rollback-worktree | 任务 2.8、3.6 区分与次数 | 一致 |
| 强制人工审核 | PR 绝对不能自动 merge | 任务 5.3 强制人工审核界面与轮询 | 一致 |
| Epic 配置路径 | 未在 FINAL 中细写 | 任务 3.2 GAP-007 补充 schema 与路径 | 已补全 |

---

## 3. 多角色审计过程摘要（含批判性审计员）

**第 1 轮（架构师 + 开发）**  
- 确认 TASKS 依赖图与阶段划分与 FINAL-COMPLETE 一致；任务 1.6/1.7 顺序已明确。  
- 开发确认 GAP-011/014 的 mock 与 fixtures 位置可实施。

**第 2 轮（批判性审计员）**  
- 质疑：GAP-004 中「>15 分」是否包含 15？逐字核对 TASKS 为「>15分：80轮+专家（不含 15 分）」，15 归属 11-15，无歧义。  
- 质疑：GAP-010 仅注释是否足够？结论：注释已标明伪代码与实现建议，验收标准含「轮询超时+提醒」，可接受。

**第 3 轮（分析师 + 测试）**  
- 分析师确认 2.2 边界值表格与 PRD/Arch 生成方式与 FINAL 一致。  
- 测试确认 6.4 容差 0.7 与「参考基准」可执行、可验收。

**第 4 轮（批判性审计员）**  
- 再次核对 GAP-001：验收标准「三个 skill 文件均成功读取，**或**异常分支已执行」是否会导致「缺失但未执行异常分支」被误判通过？结论：前置任务步骤 4 要求「将具体位置与探测结果对照」，若文件缺失则步骤 1～3 即失败，异常分支在操作步骤中已写明，执行者会先执行异常分支再写产出，故验收可接受。无新 gap。

**第 5 轮（全体）**  
- 无新 gap。批判性审计员做终审陈述（见 §1）。  

---

## 4. 结论

**结论：完全覆盖、验证通过。**

- **GAP-001～GAP-015**：在 TASKS.md 中均有对应修复标注或修改内容，逐条验证通过。  
- **与 party-mode-review 的符合性**：15 项 GAP 修复要求均已落实；优先修复项（GAP-001、002、005、007、010、011）均已在 TASKS 中体现。  
- **与 FINAL-COMPLETE 的一致性**：五层架构、复杂度矩阵、Worktree 策略、回退/回滚、强制人工审核等与设计文档一致；Epic 配置等缺失细节已通过 GAP-007 在 TASKS 中补全。  
- **实施依据**：任务列表可作为实施依据；建议实施阶段 4/3 时对 DEF-001～DEF-003 做 POC 或验证，并将结果记录于任务 7.1 的问题记录模板。

**无需「模拟修改后再审计」**：首次审计结论即为「完全覆盖、验证通过」，无遗漏章节或未覆盖要点。

---

*本报告由 §5 精神多角色审计（含批判性审计员 >30%）生成*  
*审计日期: 2026-03-02 | 审计对象: bmad-speckit-integration-TASKS.md v1.1*
