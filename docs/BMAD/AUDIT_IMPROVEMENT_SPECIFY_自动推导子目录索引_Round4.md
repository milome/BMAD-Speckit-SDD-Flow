# Specify 阶段自动推导子目录索引 — 第四轮执行阶段审计报告

**审计类型**: 执行阶段审计（改进方案文档）  
**审计依据**: audit-prompts.md §5 精神  
**审计对象**: `docs/BMAD/IMPROVEMENT_SPECIFY阶段自动推导子目录索引_100轮总结.md`  
**审计日期**: 2026-03-03  
**轮次**: Round 4  

---

## 模型选择信息

| 项目 | 值 |
|------|-----|
| 配置来源 | .claude/agents/code-reviewer.md |
| 指定模型 | inherit（来自 frontmatter，继承主 Agent 模型） |
| 选择依据 | 子代理 YAML 中 model: inherit；可改为 fast 以节省审计成本（若环境支持） |

---

## 1. R3 修复项验证

### 1.1 R3-GAP-1：键名统一为 FEATURE_NUM

| 检查项 | 结果 | 证据 |
|--------|------|------|
| 全文是否仅使用 FEATURE_NUM | ✅ 通过 | grep 全文：仅出现 FEATURE_NUM，无 SPEC_INDEX |
| 是否仍存在「SPEC_INDEX 与 FEATURE_NUM 二选一」表述 | ✅ 无 | 未发现二选一或键名混用表述 |
| 与 standalone 共用键名是否一致表述 | ✅ 通过 | §4.2 输出、任务 2、AC1 等均写「与 standalone 共用同一键名」 |

**结论**: R3-GAP-1 已在文档中**完全修复**，无新歧义。

### 1.2 R3-GAP-2：调用方义务统一为「必须」

| 检查项 | 结果 | 证据 |
|--------|------|------|
| 任务 3 是否规定「必须」 | ✅ 通过 | 「调用方**必须**在创建 story 目录后根据该输出更新 spec-index-mapping.md（或提供可执行步骤/委托给脚本）」 |
| 任务 4 是否规定「必须」 | ✅ 通过 | 「**必须**解析 FEATURE_NUM 并在创建目录后更新 spec-index-mapping.md（或委托给可执行步骤/脚本）」 |
| AC4 是否与任务 3、4 一致 | ✅ 通过 | 「调用方**必须**更新 spec-index-mapping.md（或提供可执行步骤/委托）」 |
| GAP-IDX-2 建议是否统一为「必须」 | ✅ 通过 | 「在 bmad-story-assistant 与 TASKS 中明确**必须**在创建目录后更新映射或提供可执行步骤（与任务 3、4、AC4 统一为「必须」）」 |

**结论**: R3-GAP-2 已在文档中**完全修复**，任务 3、4、AC4、GAP-IDX-2 一致，无新歧义。

---

## 2. 逐项核对（问题陈述、根因、共识、任务、AC、Deferred Gaps）

### 2.1 问题陈述（§1）

| 条目 | 核对结果 | 说明 |
|------|----------|------|
| §1.1 现状 | ✅ | 子目录命名规则、Standalone 自动推导、BMAD 未推导且依赖人工映射 — 与后文根因、方案一致 |
| §1.2 问题 | ✅ | 三点（路径无索引、映射滞后、双轨不一致）与 §2 根因、§4 共识对应 |
| §1.3 期望 | ✅ | 自动推导、路径或元数据与「索引+slug」一致或自动建立/更新映射 — 与共识方案一致 |

### 2.2 根因（§2）

| 条目 | 核对结果 | 说明 |
|------|----------|------|
| §2.1 设计层面 | ✅ | DEBATE 共识未要求嵌入 015、未规定推导并记录 — 与方案 A 不采纳、方案 B/D 采纳一致 |
| §2.2 实现层面 | ✅ | 未调用 Get-HighestNumberFromSpecs、未输出 FEATURE_NUM、未写 spec-index-mapping — 与任务 1、2、3、4 对应 |
| §2.3 流程层面 | ✅ | 更新时机未绑定自动化 — 与「由调用方执行」约定一致 |

### 2.3 共识方案（§4）

| 条目 | 核对结果 | 说明 |
|------|----------|------|
| §4.1 原则 | ✅ | 路径不变、索引单一来源、指定阶段可用 — 与任务 1～4、AC 无冲突 |
| §4.2 行为约定表 | ✅ | 推导时机、索引语义、输出 FEATURE_NUM、映射表由调用方/脚本、向后兼容 — 与任务 1～4、AC4 一致 |
| §4.3 与现有文档 | ✅ | DEBATE、create-new-feature.ps1、spec-index-mapping、speckit-workflow — 引用与任务 3、4 一致 |

### 2.4 任务列表（§5 任务 1～6）

| 任务 | 核对结果 | 说明 |
|------|----------|------|
| 1 | ✅ | BMAD 分支创建目录前调用 Get-HighestNumberFromSpecs，nextIndex 格式化 3 位 — 可执行、与 §4.2 一致 |
| 2 | ✅ | 输出增加 FEATURE_NUM（与 standalone 共用键名）— 与 §4.2、AC1 一致 |
| 3 | ✅ | speckit-workflow 说明 FEATURE_NUM + 调用方**必须**更新（或可执行步骤/委托）— 与 AC4、GAP-IDX-2 一致 |
| 4 | ✅ | bmad-story-assistant **必须**解析 FEATURE_NUM、创建目录后更新（或委托）— 与 AC4、GAP-IDX-2 一致 |
| 5 | ✅ | spec-index-mapping 或 TASKS 约定共享递增序列；落地位置（表头或 TASKS）明确 |
| 6 | ✅ | 可选小脚本/文档 — 与 AC5 映射表可更新 衔接 |

### 2.5 验收标准（AC1～AC5）

| AC | 核对结果 | 说明 |
|----|----------|------|
| AC1 | ✅ | 脚本 BMAD 输出含 FEATURE_NUM、3 位 — 与任务 1、2 一致，可验证 |
| AC2 | ✅ | 索引与现有目录无冲突、共享递增 — 与任务 5、§4.2 索引语义一致 |
| AC3 | ✅ | Standalone 不受影响 — 与 §4.2 向后兼容一致 |
| AC4 | ✅ | 文档与技能均提及 FEATURE_NUM 及「调用方必须更新（或可执行步骤/委托）」— 与任务 3、4 一致 |
| AC5 | ✅ | 映射表可更新 — 与任务 6、§4.2 映射表约定一致 |

### 2.6 Deferred Gaps（§7.2）

| ID | 核对结果 | 说明 |
|----|----------|------|
| GAP-IDX-1 | ✅ | 015 对应单个 story 还是整 Epic 的语义 — 有建议（表头或说明中明确 feature 级）；与 §7.1 条件 2、任务 5 落地位置一致 |
| GAP-IDX-2 | ✅ | 调用方未实现则滞后 — 建议已与任务 3、4、AC4 统一为「必须」，无歧义 |
| GAP-IDX-3 | ✅ | 已有 015 与新建 BMAD — 约定「新建始终分配新索引」明确 |

### 2.7 与现有文档一致性

| 引用 | 核对结果 | 说明 |
|------|----------|------|
| 参考文档路径 | ✅ | `./spec-index-mapping.md`、`./TASKS_...` 等相对路径从 `docs/BMAD/` 可解析；`spec-index-mapping.md` 已存在 |
| §4.2 与 §5、§6、§7 | ✅ | 行为约定表与任务、AC、终审条件、Deferred Gaps 无矛盾 |

---

## 3. 本轮新 Gap 检查

- 键名：全文仅 FEATURE_NUM，无 SPEC_INDEX 或二选一表述。  
- 调用方义务：任务 3、4，AC4，GAP-IDX-2 均为「必须」更新或可执行步骤/委托，表述一致。  
- 任务 5 落地位置（表头或 TASKS）与 §7.1 条件 2、GAP-IDX-1 建议一致。  
- 未发现与现有文档（DEBATE、create-new-feature.ps1、spec-index-mapping、speckit-workflow）的新冲突或遗漏。

**本轮新 gap 数量：0**

---

## 4. 结论与声明

1. **是否「完全覆盖、验证通过」**  
   **是**。R3-GAP-1、R3-GAP-2 已完全修复；问题陈述、根因、共识、任务 1～6、AC1～AC5、Deferred Gaps 与现有文档一致性逐项核对通过，无未通过项。

2. **本轮新 gap 数量**  
   **0**。

3. **连续无新 gap 计数声明**  
   本轮无新 gap。若 Round 5、Round 6 审计同样未发现新 gap，则**自本轮起可计为连续 3 轮无新 gap**（需 Round 5、6 确认）。

---

*审计员：code-reviewer（audit-prompts §5 精神）*
