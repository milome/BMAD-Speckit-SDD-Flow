---
description: 使用 bmad-bug-assistant 技能时，发起子任务前必须完成自检并输出自检结果
globs: 
alwaysApply: false
---

# BMAD Bug 助手——自检后发起

当使用 **bmad-bug-assistant** 技能且即将调用 **mcp_task** 或 **party-mode** 发起子任务时，必须先完成该阶段「发起前自检清单」全部项并在发起前**输出自检结果**（格式见技能「主 Agent 传递提示词规则」）；禁止在未完成自检或未输出自检结果的情况下发起子任务。

## Party-Mode 返回检查（Cursor）

- 若 `bmad-bug-assistant` 发起的 Cursor party-mode 子代理返回，主 Agent 必须先**单独运行** `node .cursor/hooks/party-mode-read-current-session.cjs --project-root "{project-root}"`；**禁止**把 fallback helper 拼成同一条 shell 命令，**禁止**使用 `||`、`2>&null`、`2>/dev/null`、`cmd /c`、`pwsh -c` 等回退链。仅当 `.cursor/hooks/party-mode-read-current-session.cjs` 真实不存在，或该单条命令明确返回 `MODULE_NOT_FOUND` / `ENOENT` 且指向 helper 文件本身时，才允许在**第二步单独运行** `node _bmad/runtime/hooks/party-mode-read-current-session.cjs --project-root "{project-root}"`；该 helper 会先读取 `_bmad-output/party-mode/runtime/current-session.json`。
- 主 Agent 必须先读取 helper 输出中的 `execution_evidence_level`。该字段固定为 `none|pending|partial|final`：`none` 表示当前 run 没有任何可见执行证据；`pending` 表示已有启动/进行中证据，当前 run 仍视为活跃；`partial` 表示已有部分执行证据，但还没有最终阶段 verdict；`final` 表示已有最终阶段证据或最终 verdict。在此之后，主 Agent 才能继续读取 `visible_output_summary`、`visible_fragment_record_present`、`diagnostic_classification`、`quality_flags`；**禁止**在读取这些字段之前先翻 `session log` 或 `tool-result.md`，也**禁止**使用 `ls -la`、`mkdir -p`、`dir ... /b`、`2>/dev/null` 一类目录/文件探测命令。**同样禁止**先去探测 Cursor 的 `terminals/`、`agent-transcripts/` 或其派生目录来判断 party-mode 是否成功。
- 若 helper 输出 `diagnostic_classification=degenerate_placeholder_completion`，主 Agent 只能表述为「结构完成但内容退化/机械占位」，不得误判为“提前退出”或“Task tool 全局失效”；若 helper 输出 `diagnostic_classification=stub_only_completion`，主 Agent 只能表述为「本轮只返回 completion stub」，不得仅凭这一轮就归纳为“当前环境的 Task tool 无法正确处理复杂多轮任务”。
- 若 helper 输出 `recovered_from_newer_launch = true` 或 `pending_launch_evidence_present = true`，主 Agent 必须将当前 run 视为**更新的 pending / active launch**，不得继续引用更早的 completed session 作为“本次子代理结果”，也不得据此宣称当前 run 已结束。
- 若 helper 输出 `execution_evidence_level = none`，主 Agent 只能表述为「当前 run 缺少可见执行证据，暂时不可验证」；只有这种 `none` 场景，才允许进一步怀疑 transport / host 执行链失效。
- 只有在 `visible_output_summary` 仍不足以定位问题时，才继续读取 `session_log_path`、`audit_verdict_path`、`visible_output_capture_path`。

### RCA 注记：为什么以前看起来更稳定

- 以前“看起来更稳定”，**不等于以前更健康**。早期链路更宽松，很多机械占位、单角色灌水、弱证据完成态，会被 `tool-result` 或 `audit PASS` 吞掉并表面记为成功。
- 本轮 helper 与规则加强后，这些旧问题从**隐性**变成了**显性**；因此问题数量“看起来变多”，其中一部分只是以前没被诊断出来。
- 同时，近期 party-mode 治理链路新增了 `bootstrap / current-session / post-tool-use / recovery / sidecar` 等状态面，**确实**也引入了新的同步回归风险。
- 因此主 Agent 必须把两类现象分开表述：
  - 旧问题暴露：例如 `degenerate_placeholder_completion`
  - 新回归引入：例如关键执行证据同时缺失、状态链路失配
- **禁止**把“以前看起来更稳定”概括成「以前没问题、现在都是新问题」，也**禁止**把单次 `stub_only_completion` 或 `degenerate_placeholder_completion` 泛化为「Task tool / 当前环境全局无法处理 party-mode」。
