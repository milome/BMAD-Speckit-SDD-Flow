interface:
  display_name: "Governed Feature Delivery"
  short_description: "Lightweight phased delivery with risk-based strict gates"
  default_prompt: "Use $governed-feature-delivery with a lightweight baseline by default, automatic strict risk escalation, and the selected commit, merge, or pause continuation."
