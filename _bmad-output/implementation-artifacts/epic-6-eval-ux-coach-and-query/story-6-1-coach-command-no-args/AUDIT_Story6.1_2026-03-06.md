# Story 6.1 文档审计报告

**被审文档**：`6-1-coach-command-no-args.md`  
**审计日期**：2026-03-06  
**审计依据**：覆盖需求与 Epic、禁止词、多方案共识、技术债/占位、推迟闭环、报告格式

---

## 1. 审计范围与本次修改摘要

**本次修改**：
- ① §6 禁止词表合规改为不列举禁止词
- ② 明确选定「扩展 scripts/coach-diagnose.ts」并给出理由
- ③ 删除「由 plan 确定」占位表述
- ④ 已创建 Story 6.2、6.3、6.5 文档满足推迟闭环

---

## 2. 逐项验证

### 2.1 ① Story 文档是否完全覆盖原始需求与 Epic 定义

**需求追溯**：§1 追溯表覆盖 REQ-UX-1.1～1.7；1.1～1.4 为本 Story 覆盖，1.5～1.7 明确归属 Story 6.2/6.5。

| 需求 | 覆盖/归属 | 验收 |
|------|-----------|------|
| REQ-UX-1.1 Command 入口 | 是 | AC-1 |
| REQ-UX-1.2 自动扫描、timestamp、N 条 | 是 | AC-1, AC-3 |
| REQ-UX-1.3 空目录提示 | 是 | AC-2 |
| REQ-UX-1.4 多 worktree 首版/Deferred | 是 | scope |
| REQ-UX-1.5 --epic | 否，由 6.2 | — |
| REQ-UX-1.6 --story | 否，由 6.2 | — |
| REQ-UX-1.7 Skill 自然语言 | 否，由 6.5 | — |

**Epic 6 定义**：epics.md §Epic 6 Story 6.1 与本文档 User Story、AC 一致。

**结论**：✓ 通过

---

### 2.2 ② 禁止词表：是否仍列举禁止词；正文是否存在禁止词

**§6 合规声明**（第 113–115 行）：
> 本 Story 文档已避免使用本 skill § 禁止词表所列全部词汇。所有范围界定均采用明确归属（由 Story X.Y 负责或 Deferred Gaps）。

**验证**：未列举具体禁止词，仅作总体声明。✓

**全文检索**：对禁止词「可选、可考虑、可以考虑、后续、后续迭代、待后续、先实现、后续扩展、或后续扩展、待定、酌情、视情况、技术债」全文 grep，**无命中**。

**说明**：第 103 行「同步到 `.cursor/commands/`（若项目有该约定）」为明确条件（若 X 则 Y），非禁止词「视情况」。

**结论**：✓ 通过

---

### 2.3 ③ 多方案是否已选定并记录（扩展 coach-diagnose）

**§5.2 实现路径 第 2 点**（第 98–99 行）：
> 扩展 `scripts/coach-diagnose.ts` 支持省略 `--run-id`：当未传入 `--run-id` 时执行 discovery → 取最新 run_id → 调用 coachDiagnose。**理由**：现有脚本已含 coachDiagnose 调用与输出格式，仅需增加无参分支，侵入最小、复用充分。

**验证**：已选定「扩展 coach-diagnose」方案，并给出选定理由。§3.1 第 6 点原「扩展或新增」表述已在 §5.2 收敛为单一方案。

**结论**：✓ 通过

---

### 2.4 ④ 技术债或占位表述（如「由 plan 确定」）

**全文检索**：「由 plan 确定」无匹配。

**占位/技术债类**：TBD、TODO、FIXME、待定、技术债、placeholder、后续再补 等均未检出。

**结论**：✓ 通过

---

### 2.5 ⑤ 推迟闭环：Story 6.2、6.3、6.5 是否已存在且 scope 含被推迟任务

| Story 6.1 推迟项 | 负责 Story | 文档存在 | scope 含该任务 |
|------------------|------------|----------|----------------|
| `/bmad-coach --epic N` | 6.2 | ✓ | 6.2 §3.1 明确实现 —epic N |
| `/bmad-coach --story X.Y` | 6.2 | ✓ | 6.2 §3.1 明确实现 —story X.Y |
| queryByEpic、queryByStory 等 | 6.3 | ✓ | 6.3 §3.1 实现 query 层 API |
| Skill bmad-eval-analytics 自然语言 | 6.5 | ✓ | 6.5 §3.1 自然语言触发、复用 discovery |

**文档路径**：
- `story-6-2-coach-epic-story-filter/6-2-coach-epic-story-filter.md` ✓
- `story-6-3-scoring-query-layer/6-3-scoring-query-layer.md` ✓
- `story-6-5-bmad-eval-analytics-skill/6-5-bmad-eval-analytics-skill.md` ✓

**结论**：✓ 通过

---

### 2.6 ⑥ 报告格式符合要求

本报告采用指定格式，结尾含「结论：通过/未通过」及必达子项逐项结论。

**结论**：✓ 通过

---

## 3. 综合结论

| 必达子项 | 判定 |
|----------|------|
| ① 覆盖需求与 Epic | ✓ |
| ② 禁止词表（不列举；正文无禁止词） | ✓ |
| ③ 多方案已选定（扩展 coach-diagnose） | ✓ |
| ④ 无技术债/占位表述 | ✓ |
| ⑤ 推迟闭环（6.2、6.3、6.5 存在且 scope 含被推迟任务） | ✓ |
| ⑥ 本报告结论格式符合要求 | ✓ |

---

**结论：通过。**

**必达子项**：① 覆盖需求与 Epic ✓；② 禁止词表合规（§6 不列举、正文无禁止词）✓；③ 多方案已选定（扩展 coach-diagnose，含理由）✓；④ 无技术债/占位表述 ✓；⑤ 推迟闭环 ✓（Story 6.2、6.3、6.5 已存在且 scope 明确含被推迟任务）；⑥ 本报告结论格式符合本段要求 ✓。
