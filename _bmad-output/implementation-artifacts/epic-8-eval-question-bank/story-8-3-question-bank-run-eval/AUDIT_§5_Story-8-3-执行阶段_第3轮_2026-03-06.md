# Story 8.3 执行阶段审计报告 — audit-prompts §5 第 3 轮

**审计对象**：Story 8.3（题库 run 命令与 eval_question 集成）  
**审计依据**：8-3-question-bank-run-eval.md、plan-E8-S3.md、IMPLEMENTATION_GAPS-E8-S3.md、tasks-E8-S3.md  
**实施产物**：scripts/eval-questions-cli.ts（run 子命令）、scoring/eval-questions/run-core.ts、cli-integration.test.ts、run-core.test.ts、prd、progress  
**审计日期**：2026-03-06  
**前置**：第 1、2 轮结论均为「完全覆盖、验证通过，本轮无新 gap」

---

## 1. §5 审计项逐项验证（第 3 轮复验）

| # | 审计项 | 验证方式 | 结果 |
|---|--------|----------|------|
| 1 | 是否完全覆盖 Story 文档、plan、IMPLEMENTATION_GAPS 所有章节 | 对照 Story §3 Scope、§5 Tasks、plan 4 Phases、GAP-8.3.1–8.3.6 | ✅ 覆盖 |
| 2 | 是否已执行集成测试与端到端功能测试（cli-integration.test.ts 含 run 用例） | `npx vitest run scoring/eval-questions` 35 tests 全部通过（复验 2026-03-06） | ✅ 通过 |
| 3 | 每个新增模块是否被生产代码关键路径导入并调用 | run 命令→run-core（generateEvalRunId、validateQuestionVersionForEval）→parseAndWriteScore | ✅ 已调用 |
| 4 | 是否已创建并维护 prd、progress，且每个 US 含 [TDD-RED]、[TDD-GREEN]、[TDD-REFACTOR] | 检查 prd.8-3-question-bank-run-eval.json、progress.8-3-question-bank-run-eval.txt | ✅ 已维护 |
| 5 | scenario=eval_question 时 question_version 是否必填，缺则 throw | run-core.validateQuestionVersionForEval + writer.validateScenarioConstraints | ✅ 已实现 |
| 6 | runId 格式是否为 eval-q{id}-{version}-{timestamp} | run-core.generateEvalRunId 输出 `eval-q001-v1-1730812345` 格式 | ✅ 符合 |
| 7 | 评分写入记录是否含 scenario、question_version、run_id | cli-integration.test 断言 record.scenario、record.question_version、record.run_id | ✅ 已验证 |

---

## 2. 逐项详情（第 3 轮复验）

### 2.1 Story / plan / GAPS 覆盖

- **T1**：eval-questions-cli.ts 实现 run 子命令，解析 --id、--version、--reportPath；校验必填、version ∈ {v1,v2}；loadManifest 按 id 查找；题目不存在时输出「题目 {id} 在版本 {version} 中不存在」。✅
- **T2**：支持 --reportPath；未传时使用 path.join(versionDir, entry.path)；解析失败捕获并输出「题目解析失败：<原因>」。✅
- **T3**：generateEvalRunId 生成 eval-q{id}-{version}-{ts}；validateQuestionVersionForEval 校验；parseAndWriteScore 传入 scenario=eval_question、question_version。✅
- **T4**：run-core.test.ts 6 用例覆盖 runId 格式与 question_version 校验；cli-integration.test.ts 含 run 题目不存在、缺 --id、缺 --version、端到端写入评分。✅

### 2.2 集成测试复验（2026-03-06）

```
npx vitest run scoring/eval-questions
✓ run-core.test.ts (6 tests)
✓ template-generator.test.ts (10 tests)
✓ manifest-loader.test.ts (10 tests)
✓ cli-integration.test.ts (9 tests) — 含 run 4 用例
  ✓ run 题目不存在时输出明确错误
  ✓ run 缺 --id 时输出用法
  ✓ run 缺 --version 时输出用法
  ✓ run 使用 --reportPath 与有效报告时写入评分
 Test Files  4 passed (4)
      Tests  35 passed (35)
```

### 2.3 关键路径导入链（复验）

```
main() → cmdRun()
  → loadManifest(versionDir)           [manifest-loader]
  → validateQuestionVersionForEval()   [run-core]
  → generateEvalRunId()                [run-core]
  → parseAndWriteScore()               [scoring/orchestrator]
    → writeScoreRecordSync()           [writer] → validateScenarioConstraints()
```

### 2.4 prd、progress 与 TDD 标记（复验）

- prd：4 个 US（US-001–US-004），均 passes: true。
- progress：每个 US 含 [TDD-RED]、[TDD-GREEN]、[TDD-REFACTOR]。✅

### 2.5 question_version 校验（复验）

- run-core：validateQuestionVersionForEval('eval_question', version)，缺失或空字符串时 throw。
- writer：validateScenarioConstraints(record) 在 writeScoreRecordSync 写入前调用。
- 单元测试：run-core.test.ts 覆盖缺失、空字符串、仅空格、有效值、real_dev 不校验。

### 2.6 runId 格式（复验）

- 实现：eval-${questionId}-${version}-${ts} → eval-q001-v1-1730812345
- 约定：RUN_ID_CONVENTION.md §2.2 eval-q{id}-{version}-{ts} ✅

### 2.7 写入记录字段（复验）

- 集成测试断言：record.scenario === 'eval_question'、record.question_version === 'v1'、record.run_id 匹配 /^eval-q001-v1-\d+$/。

---

## 批判审计员结论

**已检查维度**：遗漏需求点、边界未定义、验收不可执行、与前置文档矛盾、孤岛模块、伪实现/占位、TDD 未执行、行号/路径漂移、验收一致性。

**每维度结论**：

- **遗漏需求点**：逐条对照 Story §3 Scope、§5 Tasks、plan 4 Phases、IMPLEMENTATION_GAPS GAP-8.3.1–8.3.6、tasks T1–T4，实现与需求一一对应，无遗漏。
- **边界未定义**：缺 --id、缺 --version、非法 version、题目不存在、报告不存在、解析失败、question_version 缺失等边界均已在实现中处理，输出明确错误或 throw。
- **验收不可执行**：验收命令 `npx vitest run scoring/eval-questions` 已在本轮复验中执行，35 测试全部通过；集成测试在 tmpRoot 下搭建 rules/config/schema + manifest + sample 报告，等价于端到端验收。
- **与前置文档矛盾**：plan Phase 2、GAP-8.3.4 明确「报告来源采用题目 path 或 --reportPath；评审由 Command 编排」；实现与此一致，run 消费已有报告，无矛盾。
- **孤岛模块**：run-core 的 generateEvalRunId、validateQuestionVersionForEval 均被 eval-questions-cli.ts 的 cmdRun 导入并调用；parseAndWriteScore 在生产路径中被调用，无孤岛。
- **伪实现/占位**：无 TODO、预留或占位式实现；run 子命令完整实现参数解析、loadManifest、validateQuestionVersionForEval、generateEvalRunId、parseAndWriteScore 调用。
- **TDD 未执行**：progress 中 US-001–US-004 均含 [TDD-RED]、[TDD-GREEN]、[TDD-REFACTOR]；run-core.test.ts、cli-integration.test.ts 与实现一一对应。
- **行号/路径漂移**：引用路径 scripts/eval-questions-cli.ts、scoring/eval-questions/run-core.ts、cli-integration.test.ts 均存在且与审计描述一致；sample-eval-question-report.md 来自 parsers/__tests__/fixtures，路径有效。
- **验收一致性**：本轮复验执行 npx vitest run scoring/eval-questions，结果与第 1 轮审计报告一致（35 tests passed）；run 端到端用例断言 scenario、question_version、run_id 与 RUN_ID_CONVENTION 一致。

**对抗性复检**：

1. **v1/v2 双 run 隔离**：第 1 轮结论为「v2 会生成 eval-q001-v2-* 与 question_version=v2，与 v1 自然隔离；增加 v2 用例可增强回归，但不构成必须 gap」。本轮复验：runId 格式与 question_version 注入逻辑已由单元测试与单次 run 端到端验证，AC-5 满足。无新 gap。
2. **tmpRoot 依赖完整性**：cli-integration.test.ts 显式复制 scoring/rules、config/code-reviewer-config.yaml、scoring/schema/run-score-schema.json，run 在 tmpRoot 为 cwd 下执行，依赖已补齐。无新 gap。
3. **stage 固定为 prd**：sample 报告为 PRD 格式，stage=prd 匹配；本 Story 范围未要求多 stage，当前实现满足验收。无新 gap。

**本轮结论**：本轮无新 gap，第 3 轮；**连续 3 轮无 gap，审计收敛**。

---

## 4. 审计结论

**完全覆盖、验证通过**。

§5 七项审计项全部满足；批判审计员对抗性检查无阻塞发现；本轮复验执行验收命令通过。

**本轮无新 gap，第 3 轮；连续 3 轮无 gap，审计收敛**。

---

*本审计报告按 audit-prompts §5 执行阶段审计精神执行，批判审计员结论占比 >50%。*
