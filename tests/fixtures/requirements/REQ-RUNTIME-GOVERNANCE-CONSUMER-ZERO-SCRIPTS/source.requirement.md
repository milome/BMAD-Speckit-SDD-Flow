# Runtime Governance Consumer Zero Scripts Fixture

This fixture preserves the wording checked by docs contract tests without reading ignored `docs/requirements/**` runtime inputs.

不代表当前产品路径需要人工串联 post-audit close-out。

手动触发或通过测试驱动验证 `post-tool-use`。
