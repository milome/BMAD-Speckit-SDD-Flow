{
  "schemaVersion": "requirements-contract-authority-source/v1",
  "sourceRootId": "QUESTION-APPROVAL-FAILURE-OUTCOMES",
  "semanticBody": {
    "question": "审批被拒绝或长时间未完成时，批次分别应如何结束？",
    "affectedFieldIds": ["approval.failureOutcomes"],
    "affectedNodeIds": ["MUST-FR-BATCH-REFUND-APPROVAL"],
    "answerSchema": {
      "type": "object",
      "additionalProperties": false,
      "required": ["rejected", "timeoutHours", "timeout"],
      "properties": {
        "rejected": { "type": "string" },
        "timeoutHours": { "type": "integer", "minimum": 1 },
        "timeout": { "type": "string" }
      }
    }
  }
}
